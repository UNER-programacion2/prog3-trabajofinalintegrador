<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <title>Informe de Reservas</title>
    <style>
        body {
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            font-size: 12px;
            margin: 40px;
            color: #333;
        }

        h1 {
            text-align: center;
            color: #2c3e50;
            border-bottom: 2px solid #2c3e50;
            padding-bottom: 10px;
        }

        table {
            width: 100%;
            border-collapse: collapse;
            margin-top: 20px;
        }

        th {
            background-color: rgb(9, 11, 15); /* Estilo del primer archivo */
            color: #fff;
            padding: 8px;
            text-align: left;
        }

        td {
            border-bottom: 1px solid #ccc;
            padding: 8px;
            text-align: center; /* Estilo del primer archivo */
        }

        tr:nth-child(even) {
            background-color: #f8f8f8; /* Estilo del primer archivo */
        }

        .footer {
            text-align: center;
            font-size: 10px;
            margin-top: 40px;
            color: #777;
        }
    </style>
</head>
<body>
    <h1>📅 Informe de Reservas</h1>

    <table>
        <thead>
            <tr>
                {{! Columnas del segundo archivo }}
                <th>ID</th>
                <th>Salón</th>
                <th>Fecha</th>
                <th>Hora Inicio</th>
                <th>Hora Fin</th>
                <th>Usuario</th>
                <th>Importe Total</th>
            </tr>
        </thead>
        <tbody>
            {{#each reservas}}
            <tr>
                {{! Datos del segundo archivo }}
                <td>{{this.reserva_id}}</td>
                <td>{{this.nombre_salon}}</td>
                <td>{{this.fecha_reserva}}</td>
                <td>{{this.hora_desde}}</td>
                <td>{{this.hora_hasta}}</td>
                <td>{{this.usuario}}</td>
                <td>{{this.importe_total}}</td>
            </tr>
            {{/each}}
        </tbody>
    </table>

    <p class="footer">Programación III - 2025</p>
</body>
</html>